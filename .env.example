# Backend / AI Core configuration
# Copy this file to `.env` and replace the placeholder values before running the stack.
GOOGLE_API_KEY=your-google-generative-ai-key

# Either provide DATABASE_URL or all of the DB_* credentials below.
DATABASE_URL=
DB_HOST=localhost
DB_PORT=5432
DB_NAME=float
DB_USER=postgres
DB_PASSWORD=2021

# Optional tuning knobs
FAISS_INDEX_PATH=ai_core/faiss_index
RAG_RETRIEVER_K=6
BACKEND_CORS_ORIGINS=http://localhost:5173
API_HOST=0.0.0.0
API_PORT=8000
